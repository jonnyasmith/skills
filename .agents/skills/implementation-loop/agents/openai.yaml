interface:
  display_name: "Implementation Loop"
  short_description: "Implement dependent tickets one at a time"
  default_prompt: "Use $implementation-loop to implement these tickets sequentially from the linked spec."
policy:
  allow_implicit_invocation: false
