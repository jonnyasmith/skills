interface:
  display_name: "To Gauntlet"
  short_description: "Turn a goal into a Gauntlet Loop brief and driver"
  default_prompt: "Use $to-gauntlet to turn this goal into a Gauntlet Loop brief and driver I can run."
policy:
  allow_implicit_invocation: false
